@model IEnumerable<FinancePlan.Models.Household>
@using FinancePlan.Models

@{
    ViewBag.Title = "Index";
}

@{
    var db = new ApplicationDbContext();
}

<h2>Your House</h2>

@if (!User.IsInRole("Owner"))
{
    <p>
        @Html.ActionLink("Create New House", "Create")
    </p>
}


<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.ActionLink("House Members", "HouseMembers", new { id = item.ID }) |
                @Html.ActionLink("Edit", "Edit", new { id = item.ID }) |
                @Html.ActionLink("Details", "Details", new { id = item.ID }) |
                @*@Html.ActionLink("Leave", "LeaveHouse", new { id = item.ID }) |*@
                @if (User.IsInRole("Owner"))
                {
                    @Html.ActionLink("Delete", "Delete", new { id = item.ID })
                }

            </td>
        </tr>
    }

</table>
