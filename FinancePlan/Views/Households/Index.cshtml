@model FinancePlan.Models.Household
@using FinancePlan.Models

@{
    ViewBag.Title = "Index";
}

@{
    var db = new ApplicationDbContext();
}

<h2>Your House</h2>

@if (!User.IsInRole("Owner"))
{
    <p>
        @Html.ActionLink("Create New House", "Create")
    </p>
}


<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

        <tr>
            <td>
                @Html.DisplayFor(modelItem => Model.Name)
            </td>
            <td>
                @Html.ActionLink("House Members", "HouseMembers", new { id = Model.ID }) |
                @Html.ActionLink("Edit", "Edit", new { id = Model.ID }) |
                @Html.ActionLink("Details", "Details", new { id = Model.ID }) |
                @*@Html.ActionLink("Leave", "LeaveHouse", new { id = item.ID }) |*@
                @if (User.IsInRole("Owner"))
                {
                    @Html.ActionLink("Delete", "Delete", new { id = Model.ID })
                }

            </td>
        </tr>


</table>
